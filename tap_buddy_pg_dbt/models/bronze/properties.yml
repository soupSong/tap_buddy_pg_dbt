version: 2

models:
  - name: bronze_crm_students #  Must match the filename of a model -- including case sensitivity.
    config:
        materialized: view

  - name: bronze_bq_messages
    config:
      materialized: view